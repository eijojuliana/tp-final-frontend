export interface Tienda {
  tiendaId: number,
  nombre: string,
  direccion: string,
  caja: number,
  duenio: number,
}

export interface newTienda {
  nombre: string,
  direccion: string,
  caja: number,
  duenio: number,
}
